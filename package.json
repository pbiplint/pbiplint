{
  "name": "pbiplint-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test -c packages/web/playwright.config.ts",
    "test:e2e:ui": "playwright test -c packages/web/playwright.config.ts --ui",
    "typecheck": "tsc -b packages/core && tsc -p packages/cli/tsconfig.json && tsc -p packages/web/tsconfig.json && tsc -p tsconfig.test.json",
    "lint": "eslint . && prettier --check .",
    "format": "prettier --write .",
    "check:browser": "node packages/core/scripts/check-browser-bundle.mjs",
    "check:pack": "node scripts/check-pack.mjs",
    "version:sync": "node scripts/sync-version.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@eslint/js": "^10.0.1",
    "@fontsource-variable/inter": "^5.3.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "happy-dom": "^20.14.5",
    "marked": "^18.0.13",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.69.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
