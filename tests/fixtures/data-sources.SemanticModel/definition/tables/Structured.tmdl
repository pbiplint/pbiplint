table Structured

	column Id
		dataType: int64
		summarizeBy: none
		sourceColumn: Id

	partition Structured = query
		dataView: full
		source
			query = SELECT * FROM dbo.Structured
			dataSource: SQL/localhost;Sales

